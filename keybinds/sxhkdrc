# --- Applications ---
super + l
	betterlockscreen -l dim

super + space
	rofi -modi "drun,window" -show drun -show-icons

super + q
    bash -c 'xdotool getwindowfocus windowclose'

# --- Audio Controls ---
XF86AudioRaiseVolume
	pactl set-sink-volume @DEFAULT_SINK@ +5%

XF86AudioLowerVolume
	pactl set-sink-volume @DEFAULT_SINK@ -5%

XF86AudioMute
	pactl set-sink-mute @DEFAULT_SINK@ toggle

# --- JetBrains IDEs ---
super + c
	/home/royale/.local/share/JetBrains/Toolbox/scripts/clion

super + y
	/home/royale/.local/share/JetBrains/Toolbox/scripts/pycharm

super + f
	/home/royale/.local/share/JetBrains/Toolbox/scripts/fleet

super + i
	/home/royale/.local/share/JetBrains/Toolbox/scripts/idea

super + w
	/home/royale/.local/share/JetBrains/Toolbox/scripts/webstorm
